#include "LoadingScreenWidget.h"
#include "Components/Image.h"
#include "Engine/Texture.h"

/*
void ULoadingScreenWidget::SetLoadingImage(UMediaTexture* MediaTexture)
{
    if (LoadingImage && MediaTexture)
    {
        FSlateBrush Brush;
        Brush.SetResourceObject(Cast<UObject>(MediaTexture)); 
        Brush.ImageSize = FVector2D(1920.0f, 1080.0f);  // 必要に応じて画像サイズを設定
        LoadingImage->SetBrush(Brush);  // Imageウィジェットに適用
    }
}
*/