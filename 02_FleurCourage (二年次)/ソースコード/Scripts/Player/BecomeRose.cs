using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*

エラーが発生したため一時コメントアウト

public class BecomeRose : MonoBehaviour
{
    [SerializeField]
    private GameObject _closeRose;
    [SerializeField]
    private GameObject _becomeRose;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKey(KeyCode.A))
        {
            Destroy(_closeRose);
            _becomeRose.SetActive(true);
        }
    }
}
*/